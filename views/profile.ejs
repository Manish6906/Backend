<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class=" bg-gray-700 min-h-screen p-10">
        <div class="grid justify-items-end pr-6">
            <a href="/logout" class="bg-red-500 px-3  rounded-md text-white">Logout</a>
        </div>
        <h1 class="text-white text-2xl">Hello Welcome , <%=user.name %> 👋</h1>
       <div>
        <form action="/post" method="post">
            <textarea placeholder="what's going on mind ?" class="resize-none overflow-y-hidden p-2 rounded-md block w-full sm:w-1/3 mt-4 bg-gray-500 outline-0 text-white" name="content"></textarea>
            <input type="submit" class="bg-blue-500 text-white px-4 py-2 rounded-md mt-2">
        </form>
       </div>

       <div class="mt-5"><hr>
        <h5 class="mt-2 text-zinc-400  text-xl">Your Post....</h5>
        <div class="postcontainer mt-3 flex  gap-7 flex-wrap ">
           
            <% user.post.forEach(function(val){%>
                <div class="post bg-zinc-600 px-3 py-2 rounded-md w-[21rem]">
                    <h5 class="text-blue-500 tracking-tighter"><%=user.email %></h5>
                        <h5 class="text-gray-400 tracking-tighter">Name = <%=user.name %></h5>
                        <p class="text-white tracking-tighter leading-5"><%=val.content%></p>
                        <div class="mt-2 flex gap-6"></div>
                        <a href="" class="text-blue-400 "> Like</a>
                        <a href="" class="text-zinc-400 "> Edit</a>
                    </div>
                <%})
                %>
    
            
            </div>
            

            


            
        </div>
       </div>
        
    </div>
</body>
</html>